# -*- coding: utf-8 -*-

import pyinotify
import os
import sys

class InotifyWatch():

    def add(self, path, mask, rec=True, auto_add=True, exclude_filter=[]):


    def __delete__(self, instance):
